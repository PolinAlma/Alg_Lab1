#pragma once

void fill_array(int**array, const size_t size1, const size_t size2, int number);

void print_array(int** array, const size_t size1, const size_t size2);

bool equal_array(int** array1, const size_t str1, const size_t column1, int** array2, const size_t str2, const size_t column2);
